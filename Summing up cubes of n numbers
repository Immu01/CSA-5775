#include <stdio.h>

int main() {
    int n;

    printf("Enter the value of n: ");
    scanf("%d", &n);

    int sum = 0;

    for (int i = 1; i <= n; i++) {
        sum += i * i * i;
    }

    printf("Sum of the cubes of numbers from 1 to %d = %d\n", n, sum);

    return 0;
}
