#include <stdio.h>
#include <stdlib.h>

int main() {
    char hexNumber[100];
    long decimalNumber;

    printf("Enter a hexadecimal number: ");
    scanf("%s", hexNumber);

    decimalNumber = strtol(hexNumber, NULL, 16);

    printf("Decimal equivalent: %ld\n", decimalNumber);

    return 0;
}
